apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../base

resources:
  - apps.yaml
# patches:
# - path: patch.yaml
#   target:
#     kind: Application
#     labelSelector: app=manifests
